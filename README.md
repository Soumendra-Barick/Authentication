## 🔐 Environment Variables

To run this project locally, create a `.env` file in the root directory and add the following:

```env
MONGODB_URL=your_mongodb_connection_string
JWT_SECRET=your_jwt_secret_key
PORT=5000
```

### Setup

1. Create a `.env` file in the project root.
2. Copy the variables above.
3. Replace the placeholder values with your own credentials.

> ⚠️ The `.env` file is ignored via `.gitignore`. Never commit your real credentials.
